var config = require('./../config/config');
var constants = require('./../config/constants');
//var moment = require('moment');

module.exports = {
    'validation': {

    },
    'error': {
        'email_already_exists': 'Sorry! We already have an account with this Email ID',
    },
    'success': {
        'user_added': 'Successfully added the user',
    }
};
